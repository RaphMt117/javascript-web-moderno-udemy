const [a, b, c, d] = [1, 2, 3, 4];

const soma = a + b + c + d;
const subtracao = d - b;
const multiplicacao = b * 3;
const divisao = d / a;
const modulo = b % 2;

console.log(soma, subtracao, multiplicacao, divisao, modulo);
